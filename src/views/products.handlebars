{{#if isValid}}
<div>
    {{#each docs}}
    <div>
        <p>Nombre del Producto: {{this.title}} &nbsp; Precio: {{this.price}}</p>
        <p>Cantidad disponible: {{this.stock}} pzas &nbsp; Calificación: {{this.category}}</p>
    </div>
    <br/>
    {{/each}}
</div>
<br/>
<div>
    {{#if hasPrevPage}}
        <a href={{this.prevLink}}> Anterior</a>
    {{/if}}
    &nbsp;
    {{this.page}}
    &nbsp;
     {{#if hasNextPage}}
        <a href={{this.nextLink}}>Siguiente </a>
    {{/if}} 
</div>
{{else}}
<h1>No hay Productos</h1>
{{/if}}